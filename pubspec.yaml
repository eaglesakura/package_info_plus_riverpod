name: package_info_plus_riverpod
description: "Riverpod Provider for package_info_plus"
version: 1.0.0+2
repository: https://github.com/eaglesakura/package_info_plus_riverpod/

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  package_info_plus: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
